# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temporary files
tmp/
*.tmp
*.bak

# Logs — runtime output, not source code
*.log
log*.txt
terminal.txt
terminal*.txt
log.txt
cli.txt
vastai_console.txt

# ML Models — large binary files belong in cloud storage, not git
*.pkl
*.h5
*.pt
*.pth
models/

# DONUT model artifacts at repo root (downloaded at runtime from HF cache)
tokenizer.json
tokenizer_config.json
processor_config.json
generation_config.json
config.json

# Runtime results — generated by run_all.py, not source code.
# Exception: the canonical reference JSONs for the final published
# experiments are committed so paper generation works from a clean clone.
results/figures/
results/convergence_plots.tex
results/f1_barchart.tex
results/aggregated_summary.json
results/benchmark_results.json
results/evaluation_results.json
# DO NOT ignore: results/all_experiments.json, results/trocr_yolo_results.json
#   — these are the authoritative final-run data used by inject_results.py.

# Stray experiment_*.json files at repo root (should live in results/)
experiment_*.json

# Stray result JSON duplicates at repo root
all_experiments*.json
benchmark_results.json
trocr_yolo_results*.json
finetuning_params.*
seller_split_cache.json
convergence_exp*.csv

# Admin / meta documents (not project source)
PR.md
SENTINEL_PROMPT.md

# Filled paper & presentation — generated by inject_results.py
paper_filled.tex
presentation_filled.tex
paper/paper_filled.tex
paper/presentation_filled.tex
paper/paper_mini.tex
paper/paper_micro.tex

# Secrets — never commit
hf_token.txt
*_token.txt
*.secret
.env

# Dataset cache
data/

# Test / lint caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
