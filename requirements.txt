torch>=2.0.0
torchvision>=0.15.0
transformers>=4.35.0
datasets>=2.14.0
# FIX: protobuf is required by XLMRobertaConverter which is used internally
# by DonutProcessor.from_pretrained().  This single missing dependency caused
# 100% pipeline failure (all 8 experiments crashed with ImportError).
protobuf>=3.20.0
ultralytics>=8.0.0       # YOLOv8 for TrOCR+YOLO pipeline
timm>=0.9.0              # required by TrOCR vision encoder (DeiT backbone)
Pillow>=9.0.0
opencv-python>=4.7.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
jiwer>=3.0.0             # WER/CER metrics for TrOCR+YOLO evaluation
tqdm>=4.65.0
accelerate>=0.24.0
sentencepiece>=0.1.99
editdistance>=0.6.0      # NED computation in evaluate.py
psutil>=5.9.0            # RAM-aware caching in run_experiments.py
# Dev / test tools
pytest>=7.0              # required to run: pytest tests/
# flash-attn is OPTIONAL — the pipeline runs correctly without it (PyTorch 2.x
# built-in SDPA is used as fallback).  Install separately after torch if desired:
#   pip install flash-attn --no-build-isolation
# KNOWN ISSUE: flash-attn 2.x fails to build from source on torch>=2.9+cu126
# due to an nvcc segfault.  Use a prebuilt wheel finder:
#   https://flashattn.dev/wheel-finder/
# Select: Python 3.12 / CUDA 12.6 / your PyTorch version → copy the pip command.
